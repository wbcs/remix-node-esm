{
  "name": "workspace",
  "scripts": {
    "reset": "pnpm -r reset && pnpm -r clean && rimraf node_modules/"
  },
  "devDependencies": {
    "esbuild": "^0.17.16",
    "glob": "^10.1.0",
    "npm-run-all": "^4.1.5",
    "rimraf": "^5.0.0",
    "tsx": "^3.12.6",
    "typescript": "^5.0.4"
  }
}
